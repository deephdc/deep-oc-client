============
Installation
============

At the command line::

    $ pip install deep-oc-client

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv deep-oc-client
    $ pip install deep-oc-client
